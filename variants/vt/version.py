# Single source of truth for the app version. Bump this by hand per release;
# it's what the right-click context menu displays, so it's the one place to
# update rather than hunting for a hardcoded string elsewhere.
__version__ = "1.0.0"
